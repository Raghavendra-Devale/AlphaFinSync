# AlphaFinSync
AlphaFinSync is a backend service that fetches, processes, and stores stock financial data from external APIs (Alpha Vantage) into a PostgreSQL database. It provides a set of REST APIs to manage and query financial metrics such as EPS, revenue, and other quarterly/yearly data.
